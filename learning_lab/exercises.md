# Exercises (In Order)

1. Implement `AIQueryRequest` and `AIQueryResponse` in `schemas.py`.
2. Implement SQL guard in `sql_guard.py`.
3. Implement basic keyword intent parser in `service.py`.
4. Implement simple semantic index in `semantic_search.py`.
5. Implement `/ai/query` endpoint in `api.py` with mock service return.
6. Wire full flow in `service.py`: intent -> retrieval -> safe SQL -> answer.
